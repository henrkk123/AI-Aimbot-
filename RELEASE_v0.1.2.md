# 🧠 CV-Overlay Pro v0.1.2 (Auto-Heal Update)

This update revolutionizes the setup process with **Smart Launchers**.

> **Setup Required? ZERO.**

---

## ✨ What's New?

### 🛠️ Self-Healing Launchers
The `start_overlay.bat` (and `.command`) is now intelligent:
-   **Auto-Detection**: It checks if Python libraries or UI files are missing.
-   **Auto-Installation**: If anything is missing, it **automatically installs it** before launching.
-   **No More Manual Setup**: We removed `setup_windows.bat` requirement. Just run the app!

### 📚 Simplification
-   **Docs Updated**: Installation guide is now just 2 steps: "Install Prereqs" -> "Run App".
-   **Clean Repo**: Removed redundant setup scripts from documentation references.

---

## 📦 How to Update

1.  **Get latest code** (`git pull` or re-download zip).
2.  **Delete** your old `node_modules` or `.venv` if you want to test the magic (optional).
3.  **Double-Click** `start_overlay.bat`.

*It just works.* 🪄

---

**Contributors**: @henrkk123
