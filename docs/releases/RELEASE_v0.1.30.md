# ☢️ CV-Overlay Pro v0.1.30 (Nuclear Option)

> **✨ ENVIRONMENT FIX**
> "It still didn't work?"
> Then your `.venv` is corrupted or `pip` was installing to the wrong place.

## 🛠️ The Change
-   `force_install.py`: Now uses the **EXACT** python executable path to install packages. No more "wrong pip".
-   `reset_env.bat`: **NEW TOOL**. It deletes the entire `.venv` folder. A true Factory Reset.

## 📦 How to Apply
1.  **Download v0.1.30**.
2.  Run `reset_env.bat` (Lösche alles).
3.  Run `install_gpu.bat` (Installiere frisch).
4.  Launch.

*If this fails, I will eat a broom.* 🧹

---

**Contributors**: @henrkk123
