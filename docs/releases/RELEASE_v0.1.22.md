# 🎯 CV-Overlay Pro v0.1.22 (The "Pro" Update)

> **✨ AIM LOCK FIXED & DATASETS MERGED**
> "Mouse doesn't lock?" -> Fixed (Raw Input).
> "Want to merge folders?" -> Fixed (Dataset Mixer).

## 🛠️ The Fixes
1.  **Hardware Mouse (Raw Input)**:
    *   Previously: Used "Virtual Mouse" (Cursor). Games hated it.
    *   Now: Uses **Hardware Driver Level** (Relative Motion).
    *   *Result*: Locks perfectly in Fortnite, COD, Valo, etc.
2.  **Dataset Mixer**:
    *   New buttons in Trainer: **[+ Add Folder]**.
    *   Select `Dataset_Grass`, then `Dataset_Snow`, then `Dataset_City`.
    *   Click **Start**. It merges them all into one Mega-Dataset automatically. 🌪️

## 📦 How to Apply
1.  **Download v0.1.22**.
2.  Replace `gui_training.py` AND `mouse_control.py`.

*Now we are cooking.* 👨‍🍳

---

**Contributors**: @henrkk123
